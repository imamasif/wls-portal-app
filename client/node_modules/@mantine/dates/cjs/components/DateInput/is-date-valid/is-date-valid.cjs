"use client";
const require_runtime = require("../../../_virtual/_rolldown/runtime.cjs");
let dayjs = require("dayjs");
dayjs = require_runtime.__toESM(dayjs);
//#region packages/@mantine/dates/src/components/DateInput/is-date-valid/is-date-valid.ts
function isDateValid({ date, maxDate, minDate }) {
	if (date == null) return false;
	if (Number.isNaN(new Date(date).getTime())) return false;
	if (maxDate && (0, dayjs.default)(date).isAfter(maxDate, "date")) return false;
	if (minDate && (0, dayjs.default)(date).isBefore(minDate, "date")) return false;
	return true;
}
//#endregion
exports.isDateValid = isDateValid;

//# sourceMappingURL=is-date-valid.cjs.map