"use client";
const require_DatesProvider = require("./DatesProvider.cjs");
let react = require("react");
//#region packages/@mantine/dates/src/components/DatesProvider/use-dates-context.ts
function useDatesContext() {
	const ctx = (0, react.use)(require_DatesProvider.DatesProviderContext);
	const getLocale = (0, react.useCallback)((input) => input || ctx.locale, [ctx.locale]);
	const getFirstDayOfWeek = (0, react.useCallback)((input) => typeof input === "number" ? input : ctx.firstDayOfWeek, [ctx.firstDayOfWeek]);
	const getWeekendDays = (0, react.useCallback)((input) => Array.isArray(input) ? input : ctx.weekendDays, [ctx.weekendDays]);
	const getLabelSeparator = (0, react.useCallback)((input) => typeof input === "string" ? input : ctx.labelSeparator, [ctx.labelSeparator]);
	return {
		...ctx,
		getLocale,
		getFirstDayOfWeek,
		getWeekendDays,
		getLabelSeparator
	};
}
//#endregion
exports.useDatesContext = useDatesContext;

//# sourceMappingURL=use-dates-context.cjs.map