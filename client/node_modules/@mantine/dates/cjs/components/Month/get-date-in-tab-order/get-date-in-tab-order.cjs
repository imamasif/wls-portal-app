"use client";
const require_runtime = require("../../../_virtual/_rolldown/runtime.cjs");
const require_is_same_month = require("../is-same-month/is-same-month.cjs");
const require_is_after_min_date = require("../is-after-min-date/is-after-min-date.cjs");
const require_is_before_max_date = require("../is-before-max-date/is-before-max-date.cjs");
let dayjs = require("dayjs");
dayjs = require_runtime.__toESM(dayjs);
//#region packages/@mantine/dates/src/components/Month/get-date-in-tab-order/get-date-in-tab-order.ts
function getDateInTabOrder({ dates, minDate, maxDate, getDayProps, excludeDate, hideOutsideDates, month }) {
	const enabledDates = dates.flat().filter((date) => require_is_before_max_date.isBeforeMaxDate(date, maxDate) && require_is_after_min_date.isAfterMinDate(date, minDate) && !excludeDate?.(date) && !getDayProps?.(date)?.disabled && (!hideOutsideDates || require_is_same_month.isSameMonth(date, month)));
	const selectedDate = enabledDates.find((date) => getDayProps?.(date)?.selected);
	if (selectedDate) return selectedDate;
	const currentDate = enabledDates.find((date) => (0, dayjs.default)().isSame(date, "date"));
	if (currentDate) return currentDate;
	return enabledDates[0];
}
//#endregion
exports.getDateInTabOrder = getDateInTabOrder;

//# sourceMappingURL=get-date-in-tab-order.cjs.map