"use client";
const require_runtime = require("../../../_virtual/_rolldown/runtime.cjs");
let dayjs = require("dayjs");
dayjs = require_runtime.__toESM(dayjs);
//#region packages/@mantine/dates/src/components/Month/get-end-of-week/get-end-of-week.ts
function getEndOfWeek(date, firstDayOfWeek = 1) {
	let value = (0, dayjs.default)(date);
	if (!value.isValid()) return value;
	const lastDayOfWeek = firstDayOfWeek === 0 ? 6 : firstDayOfWeek - 1;
	while (value.day() !== lastDayOfWeek) value = value.add(1, "day");
	return value.format("YYYY-MM-DD");
}
//#endregion
exports.getEndOfWeek = getEndOfWeek;

//# sourceMappingURL=get-end-of-week.cjs.map