"use client";
const require_runtime = require("../../../_virtual/_rolldown/runtime.cjs");
let dayjs = require("dayjs");
dayjs = require_runtime.__toESM(dayjs);
//#region packages/@mantine/dates/src/components/MonthsList/get-months-data/get-months-data.ts
function getMonthsData(year) {
	const startOfYear = (0, dayjs.default)(year).startOf("year").toDate();
	const results = [
		[],
		[],
		[],
		[]
	];
	let currentMonthIndex = 0;
	for (let i = 0; i < 4; i += 1) for (let j = 0; j < 3; j += 1) {
		results[i].push((0, dayjs.default)(startOfYear).add(currentMonthIndex, "months").format("YYYY-MM-DD"));
		currentMonthIndex += 1;
	}
	return results;
}
//#endregion
exports.getMonthsData = getMonthsData;

//# sourceMappingURL=get-months-data.cjs.map