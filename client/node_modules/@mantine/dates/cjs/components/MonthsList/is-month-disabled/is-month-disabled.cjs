"use client";
const require_runtime = require("../../../_virtual/_rolldown/runtime.cjs");
let dayjs = require("dayjs");
dayjs = require_runtime.__toESM(dayjs);
//#region packages/@mantine/dates/src/components/MonthsList/is-month-disabled/is-month-disabled.ts
function isMonthDisabled({ month, minDate, maxDate }) {
	if (!minDate && !maxDate) return false;
	if (minDate && (0, dayjs.default)(month).isBefore(minDate, "month")) return true;
	if (maxDate && (0, dayjs.default)(month).isAfter(maxDate, "month")) return true;
	return false;
}
//#endregion
exports.isMonthDisabled = isMonthDisabled;

//# sourceMappingURL=is-month-disabled.cjs.map