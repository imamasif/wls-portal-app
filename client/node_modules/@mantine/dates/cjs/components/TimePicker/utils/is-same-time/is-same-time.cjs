"use client";
const require_split_time_string = require("../split-time-string/split-time-string.cjs");
//#region packages/@mantine/dates/src/components/TimePicker/utils/is-same-time/is-same-time.ts
function isSameTime({ time, compare, withSeconds }) {
	const timeParts = require_split_time_string.splitTimeString(time);
	const compareParts = require_split_time_string.splitTimeString(compare);
	if (withSeconds) return timeParts.hours === compareParts.hours && timeParts.minutes === compareParts.minutes && timeParts.seconds === compareParts.seconds;
	return timeParts.hours === compareParts.hours && timeParts.minutes === compareParts.minutes;
}
//#endregion
exports.isSameTime = isSameTime;

//# sourceMappingURL=is-same-time.cjs.map