"use client";
const require_runtime = require("../../../_virtual/_rolldown/runtime.cjs");
const require_is_year_disabled = require("../is-year-disabled/is-year-disabled.cjs");
let dayjs = require("dayjs");
dayjs = require_runtime.__toESM(dayjs);
//#region packages/@mantine/dates/src/components/YearsList/get-year-in-tab-order/get-year-in-tab-order.ts
function getYearInTabOrder({ years, minDate, maxDate, getYearControlProps }) {
	const enabledYears = years.flat().filter((year) => !require_is_year_disabled.isYearDisabled({
		year,
		minDate,
		maxDate
	}) && !getYearControlProps?.(year)?.disabled);
	const selectedYear = enabledYears.find((year) => getYearControlProps?.(year)?.selected);
	if (selectedYear) return selectedYear;
	const currentYear = enabledYears.find((year) => (0, dayjs.default)().isSame(year, "year"));
	if (currentYear) return currentYear;
	return enabledYears[0];
}
//#endregion
exports.getYearInTabOrder = getYearInTabOrder;

//# sourceMappingURL=get-year-in-tab-order.cjs.map