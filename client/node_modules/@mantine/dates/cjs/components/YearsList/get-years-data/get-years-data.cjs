"use client";
const require_runtime = require("../../../_virtual/_rolldown/runtime.cjs");
let dayjs = require("dayjs");
dayjs = require_runtime.__toESM(dayjs);
//#region packages/@mantine/dates/src/components/YearsList/get-years-data/get-years-data.ts
function getYearsData(decade) {
	const year = (0, dayjs.default)(decade).year();
	const rounded = year - year % 10;
	let currentYearIndex = 0;
	const results = [
		[],
		[],
		[],
		[]
	];
	for (let i = 0; i < 4; i += 1) {
		const max = i === 3 ? 1 : 3;
		for (let j = 0; j < max; j += 1) {
			results[i].push((0, dayjs.default)(new Date(rounded + currentYearIndex, 0)).format("YYYY-MM-DD"));
			currentYearIndex += 1;
		}
	}
	return results;
}
//#endregion
exports.getYearsData = getYearsData;

//# sourceMappingURL=get-years-data.cjs.map