"use client";
const require_runtime = require("../../../_virtual/_rolldown/runtime.cjs");
let dayjs = require("dayjs");
dayjs = require_runtime.__toESM(dayjs);
//#region packages/@mantine/dates/src/components/YearsList/is-year-disabled/is-year-disabled.ts
function isYearDisabled({ year, minDate, maxDate }) {
	if (!minDate && !maxDate) return false;
	if (minDate && (0, dayjs.default)(year).isBefore(minDate, "year")) return true;
	if (maxDate && (0, dayjs.default)(year).isAfter(maxDate, "year")) return true;
	return false;
}
//#endregion
exports.isYearDisabled = isYearDisabled;

//# sourceMappingURL=is-year-disabled.cjs.map