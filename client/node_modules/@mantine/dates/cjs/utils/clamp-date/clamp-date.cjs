"use client";
const require_runtime = require("../../_virtual/_rolldown/runtime.cjs");
const require_to_date_string = require("../to-date-string/to-date-string.cjs");
let dayjs = require("dayjs");
dayjs = require_runtime.__toESM(dayjs);
//#region packages/@mantine/dates/src/utils/clamp-date/clamp-date.ts
function clampDate(minDate, maxDate, date) {
	if (!minDate && !maxDate) return require_to_date_string.toDateTimeString(date);
	if (minDate && (0, dayjs.default)(date).isBefore(minDate)) return require_to_date_string.toDateTimeString(minDate);
	if (maxDate && (0, dayjs.default)(date).isAfter(maxDate)) return require_to_date_string.toDateTimeString(maxDate);
	return require_to_date_string.toDateTimeString(date);
}
//#endregion
exports.clampDate = clampDate;

//# sourceMappingURL=clamp-date.cjs.map