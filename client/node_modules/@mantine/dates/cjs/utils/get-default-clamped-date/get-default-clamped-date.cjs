"use client";
const require_runtime = require("../../_virtual/_rolldown/runtime.cjs");
const require_to_date_string = require("../to-date-string/to-date-string.cjs");
let dayjs = require("dayjs");
dayjs = require_runtime.__toESM(dayjs);
//#region packages/@mantine/dates/src/utils/get-default-clamped-date/get-default-clamped-date.ts
function getDefaultClampedDate({ minDate, maxDate }) {
	const today = (0, dayjs.default)();
	if (!minDate && !maxDate) return require_to_date_string.toDateString(today);
	if (minDate && (0, dayjs.default)(today).isBefore(minDate)) return require_to_date_string.toDateString(minDate);
	if (maxDate && (0, dayjs.default)(today).isAfter(maxDate)) return require_to_date_string.toDateString(maxDate);
	return require_to_date_string.toDateString(today);
}
//#endregion
exports.getDefaultClampedDate = getDefaultClampedDate;

//# sourceMappingURL=get-default-clamped-date.cjs.map