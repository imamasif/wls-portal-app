"use client";
import { secondsToTime, timeToSeconds } from "../time-to-seconds/time-to-seconds.mjs";
//#region packages/@mantine/dates/src/components/TimePicker/utils/clamp-time/clamp-time.ts
function clampTime(time, min, max, withSeconds = true) {
	const timeInSeconds = timeToSeconds(time);
	let minInSeconds = min ? timeToSeconds(min) : -Infinity;
	let maxInSeconds = max ? timeToSeconds(max) : Infinity;
	if (!withSeconds) {
		minInSeconds = Math.ceil(minInSeconds / 60) * 60;
		maxInSeconds = Math.floor(maxInSeconds / 60) * 60;
	}
	const clamped = secondsToTime(Math.max(minInSeconds, Math.min(timeInSeconds, maxInSeconds)));
	if (!withSeconds) return {
		...clamped,
		timeString: clamped.timeString.split(":").slice(0, 2).join(":")
	};
	return clamped;
}
//#endregion
export { clampTime };

//# sourceMappingURL=clamp-time.mjs.map