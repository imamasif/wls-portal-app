"use client";
import { toDateTimeString } from "../to-date-string/to-date-string.mjs";
import dayjs from "dayjs";
//#region packages/@mantine/dates/src/utils/clamp-date/clamp-date.ts
function clampDate(minDate, maxDate, date) {
	if (!minDate && !maxDate) return toDateTimeString(date);
	if (minDate && dayjs(date).isBefore(minDate)) return toDateTimeString(minDate);
	if (maxDate && dayjs(date).isAfter(maxDate)) return toDateTimeString(maxDate);
	return toDateTimeString(date);
}
//#endregion
export { clampDate };

//# sourceMappingURL=clamp-date.mjs.map