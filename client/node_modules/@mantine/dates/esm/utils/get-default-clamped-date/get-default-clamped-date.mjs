"use client";
import { toDateString } from "../to-date-string/to-date-string.mjs";
import dayjs from "dayjs";
//#region packages/@mantine/dates/src/utils/get-default-clamped-date/get-default-clamped-date.ts
function getDefaultClampedDate({ minDate, maxDate }) {
	const today = dayjs();
	if (!minDate && !maxDate) return toDateString(today);
	if (minDate && dayjs(today).isBefore(minDate)) return toDateString(minDate);
	if (maxDate && dayjs(today).isAfter(maxDate)) return toDateString(maxDate);
	return toDateString(today);
}
//#endregion
export { getDefaultClampedDate };

//# sourceMappingURL=get-default-clamped-date.mjs.map