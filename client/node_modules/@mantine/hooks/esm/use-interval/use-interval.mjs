"use client";
import { useCallbackRef } from "../utils/use-callback-ref/use-callback-ref.mjs";
import { useCallback, useEffect, useRef, useState } from "react";
//#region packages/@mantine/hooks/src/use-interval/use-interval.ts
function useInterval(fn, interval, { autoInvoke = false } = {}) {
	const [active, setActive] = useState(false);
	const intervalRef = useRef(null);
	const handleCallback = useCallbackRef(fn);
	const intervalValueRef = useRef(interval);
	intervalValueRef.current = interval;
	const start = useCallback(() => {
		setActive((old) => {
			if (!old && !intervalRef.current) intervalRef.current = window.setInterval(handleCallback, intervalValueRef.current);
			return true;
		});
	}, []);
	const stop = useCallback(() => {
		setActive(false);
		if (intervalRef.current) window.clearInterval(intervalRef.current);
		intervalRef.current = null;
	}, []);
	const toggle = useCallback(() => {
		setActive((current) => {
			if (current) {
				if (intervalRef.current) window.clearInterval(intervalRef.current);
				intervalRef.current = null;
				return false;
			}
			if (!intervalRef.current) intervalRef.current = window.setInterval(handleCallback, intervalValueRef.current);
			return true;
		});
	}, []);
	useEffect(() => {
		active && start();
		return stop;
	}, [active, interval]);
	useEffect(() => {
		if (autoInvoke) start();
	}, []);
	return {
		start,
		stop,
		toggle,
		active
	};
}
//#endregion
export { useInterval };

//# sourceMappingURL=use-interval.mjs.map